
  # Frozen Food Inventory App

  This is a code bundle for Frozen Food Inventory App. The original project is available at https://www.figma.com/design/ldv0VwJxMG8DfRvIIskA9z/Frozen-Food-Inventory-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  